const ENV = {
  // مسار الواجهة الخلفية (Backend) الأساسي
  API_BASE_URL: process.env.REACT_APP_API_BASE || 'https://api.atharhub.dev',
  // Hiii im basm
  // معرف العميل الخاص بـ GitHub App (Client ID)
  GITHUB_CLIENT_ID: process.env.REACT_APP_GITHUB_CLIENT_ID,
};

export default ENV;
