const ENV = {
  // مسار الواجهة الخلفية (Backend) الأساسي
  API_BASE_URL: process.env.REACT_APP_API_BASE || 'https://athar-dev-hub2-bioz.vercel.app',

  // معرف العميل الخاص بـ GitHub App (Client ID)
  GITHUB_CLIENT_ID: process.env.REACT_APP_GITHUB_CLIENT_ID || 'Iv23liosF92v4oW784M5',
};

export default ENV;
